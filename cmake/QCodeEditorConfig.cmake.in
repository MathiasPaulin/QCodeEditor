@PACKAGE_INIT@

# allow to do a find_package when QCodeEditor is used as submodule and included in the buildtree
if(NOT TARGET QCodeEditor)
    include("${CMAKE_CURRENT_LIST_DIR}/QCodeEditorTargets.cmake")
endif()
